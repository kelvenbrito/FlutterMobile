class Produtos {
  //Atributos
  String codigo;
  String descricao;
  bool concluida;
  int quantidade;
  double preco;


  //construtor da classe Tarefa
   Produtos(this.codigo, this.descricao, this.concluida, this.quantidade, this.preco);
}